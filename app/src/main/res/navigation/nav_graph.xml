<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/firstFrag">
    <fragment
        android:id="@+id/firstFrag"
        android:name="ru.aidar.menu_feature_impl.presentation.MainMenuFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_main_menu">
        <action
            android:id="@+id/action_mainMenu_to_apodDestination"
            app:destination="@id/blankFragment" />
    </fragment>
    <fragment
        android:id="@+id/blankFragment"
        android:name="ru.aidar.apod_feature_impl.presentation.ApodFragment"
        android:label="Pictures"
        tools:layout="@layout/fragment_apod_list" />
</navigation>