package ru.aidar.auth_feature_impl.data.mappers

/*
class FbUserToFbAuthMapper @Inject constructor() {

    fun mapFbUserToAuthResponse(fbUserResponse: FbUserResponse): FbAuthResponse {
        val status = if (fbUserResponse.status.apply()) AuthStatus.Ok else AuthStatus.Bad
        return FbAuthResponse(user = fbUserResponse.user, status = status)
    }
}*/
